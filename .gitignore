*-debug.log
*-error.log
**/.DS_Store
/.idea
/dist
/tmp
/node_modules
oclif.manifest.json

yarn.lock
.pnpm-store
.turbo

**/tsconfig.tsbuildinfo
**/node_modules
**/dist

# Except for the committed action bundle (release-time only, see
# scripts/build-action-bundle.mjs), which ships a real node_modules/jiti/
# copy that the blanket rule above would otherwise swallow.
!/packages/actions/bundle
!/packages/actions/bundle/**

.sfpm
.sfdx
.sf
.agents
skills-lock.json
.hive
hive.toml
.graphify*
**/graphify-out

AGENTS.md
.mcp.json
.github/hooks

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

.pi-subagents
