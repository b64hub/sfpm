*-debug.log
*-error.log
**/.DS_Store
/.idea
/dist
/tmp
/node_modules
oclif.manifest.json

yarn.lock
.pnpm-store
.turbo

**/tsconfig.tsbuildinfo
**/node_modules
**/dist

# Action bundle: built at release time, force-added to the tag commit only.
# Never committed to main. The release workflow uses `git add -f` to include it.
/packages/actions/bundle/
# esbuild metafile written alongside the bundle by build-action-bundle.mjs
# (used for license notices and reproducibility diffing) — never shipped or committed.
/packages/actions/bundle-metafile.json

.sfpm
.sfdx
.sf
.agents
skills-lock.json
.hive
hive.toml
.graphify*
**/graphify-out

AGENTS.md
.mcp.json
.github/hooks

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

.pi-subagents
